#include "main.h"

/**
 * mul - multiplies two integers
 *@x: the first number to multiply
 *@y : the second number to multiply with the first one
 *Return : the result of multiplication between x and y
 */
int mul(int x, int y)
{
	return(x * y);
}
